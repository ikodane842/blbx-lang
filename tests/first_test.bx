command = {}
command.hello = () => {
    print("hello world\n")
}

//while(
//    true,
//    () => {
//        cmd = input("[bx]:> ")
//        run = command[cmd]()
//    }
//)

if(
    if(
        true,
        () => {
            return true
        },
        () => {
            return false
        },
    )

    () => {return print("im happy")}
    () => {return print("im sad")}
)